复习模块
    数据结构：

        会话词库表：
            编号、单词数量(对比全局设置.每日复习)、单词列表(主键或单词数组都行)、各种时间

        复习会话表：
            编号、模块、类型(1每日主线进度、2无限巩固练习)、状态(进行中、完成、中断、失败)、会话词库编号、预习单词列表、各类会话进度及信息的字段、各种时间

        复习记录表：
            编号、单词编号、连对次数(从1开始，错误则为0)、模块、会话、错误次数、提醒次数、前后难度、前后复习时间、各类扩展字段、各种时间

    点击模块：

        if let session.id = 存在会话(状态.进行中, 模块, 今日)
            return 恢复会话(session.id)

        if 存在会话(状态.完成, 模块, 今日, 类型.每日主线进度)
            return 创建会话(类型.无限巩固练习, 模块, 今日)

        return 创建会话(类型.每日主线进度, 模块, 今日)

    创建会话：

        let (会话词库编号, 专属单词列表) = 获取词库(类型)

        if 类型 == 类型.无限巩固练习
            专属单词列表 = 随机打乱(专属单词列表)

        return 保存会话到数据库(模块, 类型, 状态.进行中, 会话词库编号, 专属单词列表, ....)

    按规则拿词(所需数量, 要排除的单词 = [])：
        第一层(40%)：难度降序 -> 复习时间升序 -> 含义数量升序、含义字数升序 -> 单词升序 -> 编号升序
        第二层(60%)：复习时间升序 -> 难度降序 -> 含义数量升序、含义字数升序 -> 单词升序 -> 编号升序
        其中复习时间升序是指：先未复习、再早期复习、最后近期复习，也就是模拟mysql的“order by 复习时间 asc“

        let 难词所需 = 取整(所需数量 * 0.4)
        let 久词所需 = 所需数量 - 难词所需

        let 难词列表 = 从数据库拿词(数量: 难词所需, 排除：要排除的单词, 排序：第一层)
        let 久未复习单词列表 = 从数据库拿词(数量: 久词所需, 排除：难词列表 + 要排除的单词, 排序：第二层)

        return 难词列表 + 久未复习单词列表

    获取词库：
        let 会话词库 = 查询今日最新最后那条会话词库()

        if !会话词库
            单词列表 = 按规则拿词(全局设置.每日复习, [])
            会话词库 = 新建会话词库(单词列表)

        if 全局设置.每日复习 > 会话词库.单词数量
            let 还需补充数量 = 全局设置.每日复习 - 会话词库.单词数量
            let 新增单词列表 = 按规则拿词(还需补充数量, 会话词库.单词列表)
            会话词库 = 新建会话词库(会话词库.单词列表 + 新增单词列表)

        if 全局设置.每日复习 < 会话词库.单词数量
            会话词库 = 新建会话词库(会话词库.单词列表.截取(0, 全局设置.每日复习))

        if 类型.今日主线进度
            return [会话词库.编号, 会话词库.单词列表]

        let 今日单词列表 = 会话词库.单词列表;
        let 明日单词列表 = 按规则拿词(取整(全局设置.每日复习 * 0.5), 今日单词列表)

        return [NULL, 今日单词列表.随机拿一半() + 明日单词列表]

    更改设置：
        如果 全局设置.每日复习 的数量发生变化，那么应该强行中断所有模块的会话。

    删除单词：
        如果会话过程中，遇见被删除的字段，应该无感知的跳过
        比如原本会话进度是3/50，此时去词库里删掉了5个单词
        那么再次进入会话，数字进度依旧是3/50
        但是在遇到被删除的字段时，应该跳过，比如原定的第4个单词被删了，那么第3个单词完成后，直接跳转到第5个
        比如最后一个单词被删掉了，那么直接跳转到结束状态页

    复习过程:
        每一个模块在复习过程中，应该都需要产生单词的复习记录，所有的记录应该都在同一个表里，只是模块和额外信息有所不同
        而且复习过程中，对与错都能随时更新单词的难度，错一次，难度加1，连续对5次，难度减1，最低为0，最高不限
        但是单词的复习时间有额外规则。
    模块进度：
        此模块已有已完成的会话，则显示已完成
        此模块没有已完成的会话，则显示待完成
        此模块存在进行中的会话，则显示进行中
        不再显示百分比了
    会话结果：
        完成：此次会话中，所有单词一次错误都没有的过了一遍
        失败：超时或是有错误
        中断：修改了全局设置.每日复习
    复习时间：
        按现在的规则，在模块中每次对单词的操作都将产生复习记录，同时更改单词的难度与修改时间
        但是，单词的复习时间不能轻易随之更改。
        只要某个模块完成了今天的单词复习，那么它接下来的每次会话复习的会在今天和明天的单词中随机抽取
        如果此时复习到明天的单词了，并将复习时间也改了，那么等到了明天，按照现在的排序规则，就不会遇到这批单词了
        我希望每天获取一批单词，经过几个模块的反复练习加深记忆
        因此，每个模块的会话其实有两种，一种是完成今日模块进度的会话，这种会话里的复习，只会是今日单词，因此可以在修改单词难度、修改时间的同时更新复习时间
        但是如果这个模块今日的任务已经完成了，那么再次进入这个模块，开启的会话，其实是反复练习加深记忆的会话，这种会话里会随机出现今天和明天的单词，而且不论对错都是不会更新复习时间的


