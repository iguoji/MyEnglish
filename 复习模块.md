复习模块
    数据结构：
        模块词库表：
            编号、单词数量(对比全局设置.每日复习)、单词列表(主键或单词数组都行)、各种时间
        模块会话表：
            编号、模块、类型(1每日主线进度、2无线巩固练习)、状态(进行中、完成、中断、失败)、模块词库编号、各类会话信息的字段、各种时间
        复习记录表：
            编号、单词编号、连对次数(从1开始，错误则为0)、模块、会话、错误次数、提醒次数、前后难度、前后复习时间、各类扩展字段、各种时间
    排序规则：
        复习时间升序、含义数量升序、含义字数升序、难度降序、单词升序、编号升序
        其中复习时间升序是指：先未复习、再早期复习、最后近期复习，也就是模拟mysql的“order by 复习时间 asc“
    创建词库:
        $词库 = 读取最新模块词库(今日)
        if empty($词库)
            $单词列表 = 拉取全局词库(排序规则, 全局设置.每日复习)
            $词库 = 保存模块词库(全局设置.每日复习, $单词列表)
        else
            if $词库.单词数量 != 全局设置.每日复习
                if $词库.单词数量 > 全局设置.每日复习
                    $单词列表 = $词库.单词列表.截取(全局设置.每日复习)
                    $词库 = 保存模块词库(全局设置.每日复习, $单词列表)
                else
                    // 这么追加是因为怕有些单词已经修改了复习时间，至于那些上面已经修改了的，我也没什么好办法，就这样呗
                    $新增的单词列表 = 拉取全局词库(排序规则, (全局设置.每日复习 - $词库.单词数量))
                    $单词列表 = $词库.单词列表.append($新增的单词列表)
                    $词库 = 保存模块词库(全局设置.每日复习, $单词列表)
        return $词库
    创建会话:
        $会话 = 读取模块最新会话(模块、日期)
        if empty($会话)
            return 创建会话(当前模块, 进行中, 创建词库().单词列表.截取(全局设置.每日复习))
        if $会话.状态 == 进行中
            return $会话

        $已完成的会话 = 读取模块最新会话(模块, 今日, 完成)
        if $已完成的会话 && 已完成的会话.模块词库.单词数量 == 全局设置.每日复习
            $今日单词 = 创建词库().单词列表;
            $明日单词 = 拉取全局词库(排序规则, 全局设置.每日复习, offset = 全局设置.每日复习)
            return 创建会话(当前模块, 进行中, 随机一半($今日单词) + 随机一半($明日单词))
        else
            return 创建会话(当前模块, 进行中, 创建词库().单词列表.截取(全局设置.每日复习))
    打开模块：
        $会话 = 创建会话()
        获取单词(会话.词库编号)
        恢复状态(会话.会话信息)
    更改设置：
        如果全局设置.每日复习的数量发生变化，那么应该强行中断所有模块的会话。
    模块过程:
        每一个模块在复习过程中，应该都需要产生单词的复习记录，所有的记录应该都在同一个表里，只是模块和额外信息有所不同
        而且复习过程中，对与错都能随时更新单词的难度，错一次，难度加1，连续对5次，难度减1，最低为0，最高不限
        但是单词的复习时间有额外规则。
    模块进度：
        此模块已有已完成的会话，则显示已完成
        此模块没有已完成的会话，则显示待完成
        不再显示百分比了
    会话结果：
        完成：此次会话中，所有单词一次错误都没有的过了一遍
        失败：超时或是有错误
        中断：修改了全局设置.每日复习
    复习时间：
        按现在的规则，在模块中每次对单词的操作都将产生复习记录，同时更改单词的难度与修改时间
        但是，单词的复习时间不能轻易随之更改。
        只要某个模块完成了今天的单词复习，那么它接下来的每次会话复习的会在今天和明天的单词中随机抽取
        如果此时复习到明天的单词了，并将复习时间也改了，那么等到了明天，按照现在的排序规则，就不会遇到这批单词了
        我希望每天获取一批单词，经过几个模块的反复练习加深记忆
        因此，每个模块的会话其实有两种，一种是完成今日模块进度的会话，这种会话里的复习，只会是今日单词，因此可以在修改单词难度、修改时间的同时更新复习时间
        但是如果这个模块今日的任务已经完成了，那么再次进入这个模块，开启的会话，其实是反复练习加深记忆的会话，这种会话里会随机出现今天和明天的单词，而且不论对错都是不会更新复习时间的


