# 项目
- 名称：我的英语
- 介绍：一款用来加深单词记忆的APP

## 环境
- Flutter(Dart)
- Visual Code
- Mac M1 Pro

## 版本
- v x.y.z
- 版本号以 `./my_english/pubspec.yaml` 为准
- 仅在主分支时，每次更改代码需要主动推进版本号
- 只有当前分支是 `main` 才动版本号：在 feature 等分支上只改代码，**不改 `pubspec.yaml` 的 `version`，也不改 README 里「当前仓库版本」那一行**，等合并进 main 后再一次性推进
- 修复推进z，新增推进y，颠覆及破坏性改动推x
- z和y最大为9，z满10归0同时y进1，y满10归0同时x进1，x不限大小
- 每次推进版本号需要更新项目中的版本号
- Comment信息可换行，以非技术人员的角度解释其调整
- 对应调整需要同步更新到 `README.md`
- 任何代码更改都需要主动提交git
- 只有用户主动提起，才能打上标签，并推送到远程

## 流程

- 每次思考前读取本文件加深并更新记忆
- 全程使用中文进行交流
- 代码要详细且优雅的注释
- 每次的回复内容以你的模型名+可公开版本号+本地时间开头
- 如无主动提出，禁止修改非你产生的MD文件
- 调用工具前不要输出解释性文字，直接调用。
- 需要说明时，放在工具返回结果之后，一句话讲完。
- 禁止「我先看一下 X 文件」这类预告式发言。

- 默认假设我是相关专业知识的初学者，使用通俗、直接的中文交流。
- 回复时先说明最终结果，再说明做了什么。
- 每次完成任务后，依次说明：已经完成什么、发现什么问题、会产生什么影响、建议下一步做什么。
- 出现专业术语时，必须立即用生活化语言解释，不能只罗列术语或英文缩写。
- 不要让我判断自己不了解的技术方案，应主动分析并给出明确建议。
- 除了回答我提出的问题，还要主动指出我可能没有想到的风险、遗漏和后续事项。
- 未完成、未测试或无法确认的内容必须明确说明，不能含糊带过。

## 效率

- 需要新增字段或改动公共模型时，先用一句话说明「要加什么、会牵动几处」，再动手，不要在沉默中扩大改动面。
- **不主动产出「让你看效果」的额外东西**：不主动画示意图、不主动生成截图或预览、不主动搭渲染环境。要看效果由我在真机执行，或明确点名要哪种预览。
- 命令默认前台执行，只有明确耗时的构建、安装、依赖下载才放后台，避免「后台 + 等通知」多绕一轮。
- 收尾动作（版本号、README、git 提交、记忆）在代码改完且静态分析通过后**一次性做完**，不分多轮。
- 单次任务若预计超过 15 分钟，先把大致步骤说出来再执行。

## 开发

### 本机命令

- 跑 `flutter test` 必须绕开本机那个本地代理，否则 flutter_tester 的 WebSocket 握手会被拦，报 `Invalid WebSocket upgrade request`。可运行写法：`env -u http_proxy -u HTTP_PROXY -u https_proxy -u HTTPS_PROXY no_proxy=localhost,127.0.0.1 NO_PROXY=localhost,127.0.0.1 flutter test …`
- 无头渲染时 `tester.view.physicalSize / devicePixelRatio` 才是逻辑尺寸：`physicalSize = Size(390, 844)` 必须配 `devicePixelRatio = 1`，否则逻辑宽只有 195，会误报「RenderFlex overflowed」。

### 目录

参考小程序式的页面目录式组织

### 界面

整体设计如同Tabler出品，所有组件及图标仅可使用Tabler的设计，不允许使用文字型图标代替图标

## 测试
- 如无主动提出，不主动进行测试
- 如无主动提出，不主动编写测试用例
- 真机方面，用户简单执行得到结果的操作，优先让用户去做，通过挂起给出选项的方式等待反馈。
- 非我提出不能执行卸载程序避免程序将用户数据删掉了
