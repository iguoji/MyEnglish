# 项目
- 名称：我的英语
- 介绍：一款用来加深单词记忆的APP

## 环境
- Flutter(Dart)
- Visual Code
- Mac M1 Pro

## 版本
- v x.y.z
- 版本号以 `./my_english/pubspec.yaml` 为准
- 每次更改代码需要主动推进版本号
- 修复推进z，新增推荐y，颠覆及破坏性改动推x
- z和y最大为9，z满10归0同时z进1，y满10归0同时x进1，x不限大小
- 每次推进版本号需要更新项目中的版本号
- Comment信息可换行，以产品角度解释其调整
- 对应调整需要同步更新到 `README.md`
- 收到提交请求后，打上标签，并尝试推送到远程

## 流程


- 每次思考前读取本文件加深并更新记忆
- 全程使用中文进行交流
- 代码要详细且优雅的注释
- 每次的回复内容以你的模型名+可公开版本号开头
- 如无主动提出，禁止修改非你产生的MD文件


- 默认假设我是相关专业知识的初学者，使用通俗、直接的中文交流。
- 回复时先说明最终结果，再说明做了什么。
- 每次完成任务后，依次说明：已经完成什么、发现什么问题、会产生什么影响、建议下一步做什么。
- 出现专业术语时，必须立即用生活化语言解释，不能只罗列术语或英文缩写。
- 不要让我判断自己不了解的技术方案，应主动分析并给出明确建议。
- 除了回答我提出的问题，还要主动指出我可能没有想到的风险、遗漏和后续事项。
- 未完成、未测试或无法确认的内容必须明确说明，不能含糊带过。

## 开发

### 目录

参考小程序式的页面目录式组织

### 界面

整体设计如同Tabler出品，所有组件及图标仅可使用Tabler的设计，不允许使用文字型图标代替图标

### 注释

2026-08-24 起：我已经能看懂 Dart/Flutter 代码，不再需要逐行的小程序/PHP 流程翻译式注释。
注释一律改用 Dart 官方推荐的文档注释风格（`dart doc` 规范），具体要求：

- 文档注释一律使用三斜杠 `///`，不使用 `/** ... */` 或 PHPDoc 标签（不再写 `@param`、`@return`、`@var`、`@property`）。
    - 首行是一句话概括这个类/方法/属性做什么；如果需要更多说明，空一行后再展开背景、注意事项或使用示例。
    - 参数和返回值直接依赖 Dart 的类型签名本身说明（类型、可空性、命名参数一看签名就清楚），文档正文里只在“为什么这样设计”“调用方需要注意什么”不直观时才补充说明，不逐个参数罗列类型。
    - 需要举例时，用 Dart 代码块（```` ```dart ```` ）或 `///     示例代码` 缩进示例，不再用 PHP 代码块作类比。
        ```dart
        /// 按主键读取一条会话；不存在时返回 null。
        ///
        /// 调用方通常在恢复历史进度时使用，找不到记录说明这是全新一局。
        Future<ReviewSession?> getById(int id) async {
          // ...
        }
        ```
- 类与属性注释：
    - 类注释说明这个类整体负责什么、和相邻类的关系（比如“接口”和“正式实现”的对应关系），不需要写“说明所属模块”这类样板话。
    - 属性注释只在属性含义不能从名字/类型直接看懂时才写；简单属性（如 `final int id`）可以省略注释。
- 行内注释（`//`）：
    - 只在“为什么这么写”或者“这里有一个不明显的坑/约束”时才写，不要求把每一行代码在做什么翻译一遍。
    - 显而易见的代码（如简单的字段赋值、直白的条件判断）不需要注释。
- 已有代码中大量存在的旧版 PHPDoc 风格注释（`@param`/`@return`/“类似 PHP 里的 xxx”/“像小程序里的 xxx”这类表达）视为历史遗留，按接触到的文件顺手改成新风格即可，不要求一次性全项目重写。


## 测试
- 如无主动提出，不主动进行测试
- 如果你有启动、热更新、重启、关闭真机或模拟器的能力，为方便审阅或测试，你可以自行操作，比如截图、点击、模拟手势等等都能自主执行
